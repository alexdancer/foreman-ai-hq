# Planning Chat implementation history

The original milestone plan has shipped and its early proxy, Spec-artifact, and
Scout assumptions were superseded during implementation.

Use [the Planning Chat design](design/planning-chat.md) for a concise history
pointer. Current behavior is owned by `CONTEXT.md` and the canonical OpenSpec
specifications for `planning-conversation`, `planning-chat-ui`,
`markdown-task-intake`, and `orchestrator-runtime`. ADR-0009 through ADR-0011
record the runtime, model-authority, intake, and Scout-retirement decisions.
